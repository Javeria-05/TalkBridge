specVersion: "v1"
serviceId: "talkbridge"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "talkbridge-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
